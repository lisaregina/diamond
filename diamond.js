
function diamond(alpha, end){        //alpha -> array für alle buchstaben im Alphabet
    const end;
    
    // alpha.indexOf(end);
    
    // const letter = alpha.map(
    // alpha.slice(0, end +1)); //end+1, weil sonst der gegebene Buchstabe sonst nicht enthalten ist

}