const {diamond} = require("./diamond");
describe("diamond", () =>{
    it("should have letter, given from end, in alpha", ()=>{
        console.log(end = D);
        expect(diamond(alpha, D)).toEqual(D)
    )}
)};